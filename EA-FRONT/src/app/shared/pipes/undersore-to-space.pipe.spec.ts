import { UndersoreToSpacePipe } from './undersore-to-space.pipe';

describe('UndersoreToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new UndersoreToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});

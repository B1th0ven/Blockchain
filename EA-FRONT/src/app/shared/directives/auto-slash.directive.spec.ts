import { AutoSlashDirective } from './auto-slash.directive';

describe('AutoSlashDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoSlashDirective();
    expect(directive).toBeTruthy();
  });
});

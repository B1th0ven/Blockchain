import { DecrementsFilterPipe } from './decrements-filter.pipe';

describe('DecrementsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DecrementsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

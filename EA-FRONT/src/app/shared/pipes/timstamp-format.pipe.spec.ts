import { TimstampFormatPipe } from './timstamp-format.pipe';

describe('TimstampFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TimstampFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
